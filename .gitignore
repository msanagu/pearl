# Dependencies
node_modules/

# Build output
dist/
storybook-static/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# Vanilla-extract / Vite caches
.vite/

# Local agent config
.claude/

# Claude Design handoffs — local-only source material (moodboards, exports,
# scratch assets), not tracked in version control.
design/

# Private working notes and scratch spikes — the journal, the open-questions
# tracker, the full dated ADRs, and throwaway experiments. Kept local; the
# public digest of these decisions is DECISIONS.md.
docs/process/
experiments/

# Compressed originals of unused public/images assets — kept locally in case
# a future story wants them, but not shipped (nothing in src/ references
# them). See the perf pass that moved them out of public/.
/unused-images-archive/

# Hide portfolio related draft material
src/templates/portfolio/